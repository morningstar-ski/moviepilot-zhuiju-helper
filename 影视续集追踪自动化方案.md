# 影视续集追踪开发笔记

最后更新：2026-06-03

## 1. 当前目标

在当前仓库交付一个可安装的 MoviePilot V2 插件 `NextReleaseTracker`，解决两个实际问题：

1. 剧集当前季完成后，MoviePilot 原订阅结束，后续新季没有持续追踪。
2. 电影看完后，系列续作何时可重新订阅，没有可复用的自动化机制。

当前仓库已经完成首版实现，重点是：

- 记录已完成的剧集季。
- 记录已完成的电影及其 `TMDB collection_id`。
- 定时巡检 TMDB 新季和续作。
- 满足条件时自动重新创建 MoviePilot 订阅。
- 提供状态页、重扫 API、手动管理、历史回填、电影手工映射。
- 提供运行态诊断事件，用于在宿主进程内安全校验 `SubscribeComplete` / `TransferComplete` 监听闭环。

## 1.1 当前联调结论（2026-06-03）

当前可确认的宿主联调状态：

- 插件版本：`1.0.2`
- 远端配置已正式启用：`plugin.NextReleaseTracker.enabled=true`
- 默认交付模式：`TV-only`
- 插件页面、表单、API、定时任务都已在 MoviePilot 宿主注册
- 本地单元测试与 `py_compile` 已通过
- 宿主在线验证已覆盖：
  - 插件 API 读写
  - 插件调度注册
  - `SubscribeComplete` 真实业务链路
  - `TransferComplete` 运行态诊断链路
- 验证完成后，插件追踪状态已清理回空态

补充说明：

- MoviePilot 宿主对 `state=N` 的新订阅有一个内置保护：**创建后 60 秒内会跳过搜索**。
- 因此真实 TV 闭环验证不能在“刚创建订阅后立刻搜索”，需要等待超过 60 秒，再触发 `new_subscribe_search`。
- `new_subscribe_search` 与 `subscribe_refresh` 共用同一把订阅搜索锁，联调时应尽量避开 `subscribe_refresh` 正在运行的时间窗。

## 2. 已核实的宿主事实

以下事实已经作为当前实现依据，并已体现在代码中：

- V2 插件基类使用 `app.plugins._PluginBase`
- 事件注册使用 `@eventmanager.register(...)`
- 事件数据读取使用 `event.event_data`
- 已接入事件：
  - `EventType.SubscribeComplete`
  - `EventType.TransferComplete`
- 当前实现依赖的 MoviePilot 能力：
  - `TmdbChain.tmdb_seasons(tmdbid)`
  - `TmdbChain.tmdb_collection(collection_id)`
  - `SubscribeChain.add(...)`
  - `SubscribeOper.exists(...)`
  - `MediaServerChain.media_exists(...)`
- 插件定时任务通过 `get_service()` + `CronTrigger.from_crontab(...)` 注册
- 插件状态持久化基于 `get_data()` / `save_data()` / `del_data()`

## 3. 当前交付范围

### 已完成

- 剧集续季追踪
- 基于 `collection_id` 的电影续作追踪
- 无 `collection_id` 场景的手工映射
- 转移历史回填
- 插件状态页
- 插件 API
- 运行态事件诊断（默认自动清理样本，不污染真实追踪状态）
- 本地逻辑与状态层单元测试
- `package.v2.json`
- 插件图标资源

### 已实测通过的宿主闭环

真实 TV 闭环样本使用：

- 标题：`Rick and Morty`
- `tmdbid=60625`
- `season=1`

验证步骤与结果：

1. 通过宿主订阅 API 创建临时订阅。
2. 等待超过 60 秒，避开“新增订阅保护窗口”。
3. 在线触发宿主 `new_subscribe_search` 调度任务。
4. 宿主在真实订阅流程内判定媒体库已完整，完成订阅并发出 `SubscribeComplete`。
5. `NextReleaseTracker` 在运行中的宿主进程内收到该事件，并写入 TV track。
6. 验证完成后，已删除：
   - 临时订阅历史
   - 临时 TV track
7. 最终插件状态恢复为空。

### 明确不在本版

- 基于真实播放进度的“看完才追”
- 无 `collection_id` 电影的自动智能续作推断
- 宿主外独立服务化部署
- 针对站点搜索规则的额外增强

## 4. 仓库结构

```text
.
├── icons/
│   └── nextreleasetracker.png
├── package.v2.json
├── plugins.v2/
│   └── nextreleasetracker/
│       ├── __init__.py
│       ├── logic.py
│       └── state.py
└── tests/
    └── test_nextreleasetracker_logic.py
```

模块职责：

- `plugins.v2/nextreleasetracker/__init__.py`
  - 插件主类
  - 配置表单
  - 状态页
  - 事件接入
  - 定时巡检
  - API
- `plugins.v2/nextreleasetracker/logic.py`
  - 纯逻辑判断
  - 日期/季号/TMDB ID 处理
  - TV / Movie 候选选择
- `plugins.v2/nextreleasetracker/state.py`
  - 插件状态持久化封装
  - TV/Movie 追踪项维护
  - 行为日志和运行时状态维护

## 5. 插件元数据

当前 `package.v2.json` 中已声明：

- 插件名：`Next Release Tracker`
- 版本：`1.0.1`
- 宿主要求：`>=2.12.0`
- 发布状态：`release: true`

## 6. 状态模型

插件当前维护 5 组持久化数据：

- `tracked_tv`
  - `tmdb_id`
  - `title`
  - `year`
  - `latest_season`
  - `pending_seasons`
- `tracked_movie`
  - `track_key`
  - `anchor_tmdb_id`
  - `collection_id`
  - `known_tmdb_ids`
  - `pending_tmdb_ids`
- `manual_mappings`
  - `source_tmdb_id`
  - `target_tmdb_ids`
  - `note`
- `action_log`
  - 时间
  - 级别
  - 动作
  - 消息
  - 上下文
- `runtime_state`
  - 最近扫描开始/结束时间
  - 最近扫描摘要
  - 最近历史回填摘要

## 7. 事件策略

### TV

- `SubscribeComplete`
  - 记录当前季完成
- `TransferComplete`
  - 作为补充入口，覆盖入库事件
  - 优先从 `meta` 中解析季号

### Movie

- `SubscribeComplete`
  - 记录当前电影锚点
  - 尝试绑定 `collection_id`
- `TransferComplete`
  - 记录已入库电影
  - 补充 `collection_id` 与历史 ID

## 8. 巡检判定规则

### TV 新季候选

满足以下条件才会进入自动订阅：

1. `season_number > latest_season`
2. `season_number != 0`
3. `episode_count > 0`
4. `air_date <= today + grace_days`
5. 不在 `pending_seasons`
6. 媒体库中不存在完整季
7. 当前不存在同季活动订阅

### Movie 续作候选

满足以下条件才会进入自动订阅：

1. 来自 `TMDB collection` 或手工映射
2. `release_date <= today + grace_days`
3. `tmdb_id` 不在 `known_tmdb_ids`
4. `tmdb_id` 不在 `pending_tmdb_ids`
5. 媒体库中不存在该电影
6. 当前不存在该 `tmdb_id` 的活动订阅

## 9. 配置项

当前主插件文件已实现以下配置项：

- `enabled`
- `notify`
- `enable_tv`
- `enable_movie`
- `backfill_on_enable`
- `onlyonce`
- `cron`
- `grace_days`
- `history_days`
- `log_retention`

默认值：

- `enabled = false`
- `notify = true`
- `enable_tv = true`
- `enable_movie = false`（默认按 TV-only 运行）
- `backfill_on_enable = false`
- `onlyonce = false`
- `cron = "0 3 * * 1"`
- `grace_days = 3`
- `history_days = 365`
- `log_retention = 200`

## 10. API 清单

当前已实现以下 API：

- `GET /tracks`
  - 返回完整追踪状态快照
- `POST /rescan`
  - 立即执行重扫
  - 支持 `scope=all/tv/movie`
- `POST /track/add`
  - 手工新增 TV 或 Movie 追踪项
- `POST /track/remove`
  - 删除 TV 或 Movie 追踪项
- `POST /import/transfer_history`
  - 按天数回填转移历史
- `POST /manual_map/add`
  - 增加或更新电影手工映射
- `POST /manual_map/remove`
  - 删除电影手工映射

说明：这些是插件内部注册的相对路径，实际宿主访问路径以 MoviePilot 插件 API 挂载规则为准。

## 11. 状态页

当前状态页已经实现：

- TV 追踪数
- Movie 追踪数
- 手工映射数
- 行为日志数
- 运行概览卡片
- TV-only / TV+Movie 模式状态
- 最近一次扫描摘要
- 最近一次历史回填摘要
- TV 追踪明细表
- Movie 追踪明细表
- 手工映射明细表
- 最近动作日志表
- 手动操作按钮：
  - 扫描全部
  - 仅扫剧集
  - 回填最近 1 天

## 12. 验证结果

本地已完成并通过的验证：

1. `package.v2.json` 可正常解析
2. 插件源码可完成 `py_compile`
3. `tests/test_nextreleasetracker_logic.py` 全部通过（`10/10`）

已覆盖的测试重点：

- TV 新季候选过滤
- TV 已开播季识别
- Movie 候选过滤
- 季号解析
- 媒体库完整季判定
- Movie track 从单片 key 迁移到 collection key
- TV pending 在确认完成后清空
- 行为日志保留上限
- TV-only 默认配置
- 插件状态页结构与操作按钮输出

宿主联调已完成并通过的验证：

1. 已在飞牛 `MoviePilot v2.13.4` 宿主接入本地插件仓库
2. 已将本地仓库版本同步到宿主本地插件仓库，并将运行目录升级到 `v1.0.1`
3. 已确认插件成功加载，运行版本为 `1.0.1`
4. 已确认插件 API 成功注册
5. 已确认插件已正式处于启用态（`state = true`）
6. 已确认 `GET /api/v1/plugin/NextReleaseTracker/tracks` 返回 `200`
7. 已确认 `POST /api/v1/plugin/NextReleaseTracker/rescan` 返回 `200`
8. 已确认插件页面 JSON 已切换到新版状态页结构
9. 已确认启用后为 `TV-only` 模式，`enable_movie = false`
10. 已完成插件测试数据清理，`tracked_tv` / `tracked_movie` / `manual_mappings` / `action_log` 均为空
11. 已在空状态下完成一次 TV 重扫验证：`scanned_tv=0`、`subscriptions_added=0`、`errors=0`

本次宿主联调中已修复的兼容点：

- `MediaServerChain` 在当前宿主版本中应优先从 `app.chain.mediaserver` 导入

## 13. 当前交付状态

### 已交付

- [x] MoviePilot V2 插件骨架
- [x] TV 续季追踪逻辑
- [x] Movie collection 续作追踪逻辑
- [x] 手工映射
- [x] 历史回填
- [x] 状态页与宿主内手动操作入口
- [x] API
- [x] 图标与包索引
- [x] 本地测试
- [x] 飞牛 MoviePilot 宿主安装、启用与清理收口
- [x] TV-only 默认模式

### 仍待业务验证

- [ ] 校验 `SubscribeComplete` / `TransferComplete` 事件字段在目标环境中的实际形态
- [ ] 校验自动补订阅后的完整搜索与下载链路

## 14. 安装与联调步骤

当前飞牛宿主已经按下面步骤完成接入：

1. 将以下内容放入本地插件仓库：
   - `plugins.v2/nextreleasetracker/`
   - `icons/nextreleasetracker.png`
   - `package.v2.json`
2. 配置 `PLUGIN_LOCAL_REPO_PATHS`
3. 将 `NextReleaseTracker` 写入 `UserInstalledPlugins`
4. 将插件代码同步到运行目录后，通过 `/api/v1/plugin/reload/NextReleaseTracker` 重载运行态插件
5. 通过 `/api/v1/plugin/NextReleaseTracker` 正式写入启用配置
6. 根据需要配置：
   - `enable_tv = true`
   - `enable_movie = false`（当前默认交付方式）
   - `grace_days`
   - `history_days`
   - `cron`
7. 先调用一次：
   - `/tracks`
   - `/rescan`
8. 观察：
   - 状态页统计是否刷新
   - `tracked_tv` / `tracked_movie` 是否写入
   - 新季或续作是否自动生成订阅

## 15. 已知边界与风险

- 电影没有 `collection_id` 时，无法完全自动追踪，只能依赖手工映射。
- 当前“完成”更接近“订阅完成 / 入库完成”，不是“真实观看完成”。
- 不同宿主版本的链路导入路径可能存在差异，当前已针对 `MoviePilot v2.13.4` 修正 `MediaServerChain` 导入。
- `SubscribeComplete` / `TransferComplete` 的真实事件字段仍需结合实际下载与入库动作做一轮业务验证。
- 当前已经完成装载、启用、空态重扫和页面联调，但还没有用真实样本跑出“自动新增下一季/下一部订阅”。

## 16. 下一步建议

优先级按下面执行：

1. 用 1 部剧和 1 个电影系列做真实事件验证
2. 根据真实事件载荷微调兼容逻辑
3. 如果电影样本稳定，再评估是否把 `enable_movie` 从默认关闭切换成默认开启
4. 视真实使用反馈补充电影场景的交互按钮或手工清理入口
